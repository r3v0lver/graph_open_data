app:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/graphdb
    username: postgres
    password: 123456
    pool-size: 30
    test-while-idle: true
    test-on-borrow: true

server:
  servlet:
    session:
      cookie:
        same-site: "none"
  port: 3000

spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  security:
    oauth2:
      client:
        registration:
          okta:
            client-id: rfRsDpGjGlddP5GltsKMYXI6ETysqxdX
            client-secret: Cv-EUvu1plCGqkOoI8AbGu0BdkzQzkK4N5CdKWcJuJp1qmcCyVZM621due7KGhqE
            scope: openid,profile,email
        provider:
          okta:
            issuer-uri: https://dev-estc7npco1flmmpv.us.auth0.com/

# The sample and instructions above for the callback and logout URL configuration use port 3000.
# If you wish to use a different port, change this and be sure your callback and logout URLs are
# configured with the correct port.
